================================================================================
Athar — THIRD-PARTY NOTICES (placeholder)
================================================================================

This file is generated by `make notices` (scripts/gen-notices.sh) from the
real Go module graph (`go list -m -json all`) and the npm production
dependency tree — never hand-edited. This placeholder exists so the file has
content before the first `make notices` run; regenerate it before cutting a
release.

Direct dependencies as of this writing:

Go (github.com/vul-os/athar, module "athar"):
  - modernc.org/sqlite            ISC          pure-Go SQLite driver (no cgo)
  - github.com/jackc/pgx/v5       MIT          Postgres driver
  - github.com/oschwald/maxminddb-golang
                                   ISC          in-process MaxMind .mmdb reader
  - golang.org/x/crypto           BSD-3-Clause argon2id password hashing

npm (dashboard, src/):
  - react                         MIT
  - react-dom                     MIT
  - tailwindcss                   MIT
  - @tailwindcss/vite             MIT
  - vite                          MIT
  - @vitejs/plugin-react          MIT
  - esbuild                       MIT

Every one of these licences (ISC, MIT, BSD-3-Clause) permits redistribution
with the copyright notice and licence text intact and requires nothing more;
`make notices` reproduces that text in full for each package and every
transitive dependency of both graphs, and fails the build if any package in
either tree has no discoverable licence file.

Run `make notices` to replace this file with the real output.
